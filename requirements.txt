# Core drone SDK
tellopy==0.6.0

# Video decoding
av==12.3.0

# Display
pygame==2.6.1

# Deep learning (CPU or CUDA — install CUDA variant separately if GPU available)
torch==2.4.1
transformers==4.46.2
huggingface-hub==0.26.2
safetensors==0.4.5
tokenizers==0.20.3

# Image processing
opencv-python==4.10.0.84
Pillow==7.0.0
numpy==1.24.4
scipy==1.10.1
matplotlib==3.7.5

# Physics simulation (offline mode)
pybullet==3.2.6

# Control
simple-pid==2.0.1

# Serial communication (UWB)
pyserial==3.4

# Utilities
tqdm==4.67.0
typing-extensions==4.12.2

# Testing
pytest>=7.0.0

# NOTE: ROS dependencies (rospy, nlink_parser, vrpn_client_ros) are system packages
# installed via apt and catkin. They are not pip-installable.
# See README.md Installation section for ROS setup instructions.
